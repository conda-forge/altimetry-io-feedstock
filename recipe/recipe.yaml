schema_version: 1

context:
  version: "0.2.1"
  python_min: "3.11"

package:
  name: altimetry-io
  version: ${{ version }}

source:
  url: https://pypi.org/packages/source/a/altimetry-io/altimetry_io-${{ version }}.tar.gz
  sha256: 01415494f615c046631fb1713bb4d53f0b678eab1daa9805330e189e2ec5d367

build:
  noarch: python
  number: 0
  script:
    - python -m pip install . -vv

requirements:
  host:
    - python ${{ python_min }}.*
    - pip
    - setuptools
    - setuptools-scm
  run:
    - python >=${{ python_min }}
    - files_collections
    - fsspec
    - numpy
    - pandas
    - xarray

tests:
  - python:
      pip_check: true
      imports:
        - altimetry.io
      python_version:
        - ${{ python_min }}.*
        - "*"

about:
  summary: "Altimetry.io — Provide a unified way to read a data collection, independent of the underlying data representation."
  license: Apache-2.0
  homepage: https://github.com/robin-cls/altimetry_io
  repository: https://github.com/robin-cls/altimetry_io
  license_file: LICENSE

extra:
  recipe-maintainers:
    - annesophie-cls
    - thomas-z
    - robin-cls
